import java.io.IOException;

public class MainController {
    public void newGame() throws IOException {
        MainMenu.getInstance().levelChoose();
    }
    public void exit() {
        System.exit(0);
    }
}
